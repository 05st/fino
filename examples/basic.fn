import abc::xyz
import base::v1::prelude

export module abc::xyz
export factorial
export thing

fn factorial : u32 -> u32
    x = if x then '1' else factorial x 420.69

fn add : u32 -> u32 -> u32
    a b = a

let thing : bool = false
