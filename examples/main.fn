import mandelbrot
import factorial
import stl.prelude

let get_name : str =
    print_str "What is your name?\nMy name is: " >> readline

let main : unit =
    let name = get_name in
    mandelbrot
    >> println_int (10!)
    >> println_str ("Hello, " ++ name ++ "!")
